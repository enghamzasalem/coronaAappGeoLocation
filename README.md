# coronaAappGeoLocation
```
npm i 
ionic serve 
```

and add firbase API keys in index.html
```
  <script>
      // Initialize Firebase
      var config = {
        
      };
      firebase.initializeApp(config);
    </script>
    ```
